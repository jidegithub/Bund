defmodule BundWeb.LayoutViewTest do
  use BundWeb.ConnCase, async: true
  import BundWeb.LayoutView




  test "is_active_module?" do
    # Need to setup conn well as it is currently throwing errors
    # conn = build_conn(:get, "/")
    # assert is_active_module?(conn, "Dashboard") == true

  end
end
